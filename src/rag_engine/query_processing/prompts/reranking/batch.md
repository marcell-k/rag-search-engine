Rank the filing excerpts listed below by relevance to the following search query.

Query: "{query}"

Excerpts:
{doc_list_str}

Return the chunk IDs in order of relevance, best match first.

Your response must contain a raw JSON array enclosed within a `<ranking>` and `</ranking>` XML tag.
Do not wrap the JSON in Markdown backticks. Do not include any introductory or explanatory text outside the XML tags.

For example:
<ranking>["a1b2c3", "d4e5f6", "g7h8i9"]</ranking>

Ranking:
